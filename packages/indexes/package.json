{
  "name": "firestore-indexes",
  "version": "2.0.0",
  "description": "Manage and generate Firestore indexes using typescript",
  "files": [
    "bin",
    "dist/lib",
    "src/lib",
    "README.md",
    "package.json"
  ],
  "main": "dist/lib/index.js",
  "bin": {
    "firestore-indexes": "bin/firestore-indexes.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:freshfox/firestore-storage.git"
  },
  "author": "Dominic Bartl",
  "scripts": {
    "test": "NODE_ENV=test mocha dist/test",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "build": "yarn clean && tsc --build",
    "fss": "fss"
  },
  "license": "MIT",
  "devDependencies": {
    "should": "^13.2.3",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "ts-object-path": "^0.1.2"
  }
}
