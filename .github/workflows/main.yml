name: Firestore Storage CI

on: [push]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: [ core, firestore, indexes ]
        node: [ 14 ]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node }}
      - uses: actions/cache@v2
        id: modules-cache
        with:
          path: 'packages/${{ matrix.package }}/node_modules'
          key: ${{ runner.os }}-api-node-modules-${{ matrix.node }}-${{ hashFiles('packages/${{ matrix.package }}/yarn.lock') }}
      - name: Install dependencies
        if: steps.modules-cache.outputs.cache-hit != 'true'
        run: cd ${{ matrix.package }} && yarn install
      - name: Build package
        run: cd ${{ matrix.package }} && yarn build
      - name: Build test
        run: cd ${{ matrix.package }} && yarn test
